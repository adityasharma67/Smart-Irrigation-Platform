{
  "version": 2,
  "builds": [
    {
      "src": "smart-irrigation-platform/frontend/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "build" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "smart-irrigation-platform/frontend/build/$1" }
  ]
}
