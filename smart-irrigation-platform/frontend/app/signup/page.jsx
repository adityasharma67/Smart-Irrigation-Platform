import { SignupPanel } from "@/components/auth/AuthPanel";

export const metadata = { title: "Create workspace | Smart Farming Hub" };
export default function SignupPage() {
  return <SignupPanel />;
}
